//#include "Experiment.hpp"

//#include <fstream>

//#include <xtensor/xarray.hpp>
//#include <xtensor/xcsv.hpp>
//#include <xtensor-fftw/basic.hpp> // rfft, irfft

//double calculate_amplitude(xt::xarray<double> &waveform, double time_inter)
//{
////	size_t n = waveform.shape(0);
////	auto fft = xt::fftw::rfft(waveform);

////	xt::xarray<double> ampl = xt::abs(fft) / n;
////	xt::xarray<double> amax = xt::amax(ampl);

////	return amax(0) * 2;
//    return 0;
//}
