#include "Experiment.hpp"

#include <iostream>

int main(int argc, char const *argv[])
{
    Experiment exp;
    // exp.calculate_amplitude();
    exp.resonance_curve();

    // for (size_t i = 0; i < data.first.size(); i++)
    // {
    //     std::cout << data.first[i] << " " << data.second[i] << std::endl;
    // }

    return 0;
}
